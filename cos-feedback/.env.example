# Optional local reference — the app reads constants from app.js (not .env on GitHub Pages).
# Supabase → Project Settings → API

SUPABASE_URL=https://YOUR_PROJECT_ID.supabase.co
SUPABASE_KEY=YOUR_ANON_KEY
